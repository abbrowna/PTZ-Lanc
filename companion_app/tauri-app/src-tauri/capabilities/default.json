{
  "identifier": "default",
  "description": "Default capabilities – allows invoke(), event.listen(), and opening new camera windows.",
  "windows": ["main", "camera-*"],
  "permissions": [
    "core:default",
    "core:window:allow-create"
  ]
}
